// 'use strict';

// patchNotesApp.filter("PatchFilter", function() {
//     return function(newsArray, tags) {
//         return newsArray.filter(function(news) {
//             for (var i in news.title) {
//                 if (tags.indexOf(news.title[i]) != -1) {
//                     return true;
//                 }
//             }
//             return false;

//         });
//     };
// 	// let patchFilter = (tags) => {
// 	// 	console.log("tags", tags);
// 	// 	for(let i = 0; i < tags.length; i++) {
// 	// 		if(news.title.toUpperCase().indexOf(tags[i].toUpperCase()) >= 0) {
// 	// 			return true;
// 	// 		}
// 	// 	}
// 	// 	return false;
// 	// };

// 	// return {patchFilter};

// });
